# RHP Analyzer Environment Variables
#
# Copy this file to '.env' and set your actual values
#
# Environment variables take precedence over config.yaml settings
# Use the RHP_ prefix with double underscores for nested values
# Example: RHP_PATHS__OUTPUT_DIR overrides paths.output_dir in config.yaml
#
# ============================================================================

# ============================================================================
# REQUIRED SETTINGS
# ============================================================================

# Hugging Face API Token (REQUIRED for LLM access)
# Get your token from: https://huggingface.co/settings/tokens
# Required for: Qwen, Llama, and other HuggingFace models
HF_TOKEN=your_huggingface_token_here

# ============================================================================
# COMMONLY CHANGED SETTINGS
# ============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Use DEBUG for troubleshooting, INFO for normal operation
RHP_LOG_LEVEL=INFO

# Embedding device (cpu or cuda)
# Use cuda if you have an NVIDIA GPU for 10-20x speedup
# RHP_EMBEDDING__DEVICE=cuda

# Embedding model selection
# Uncomment to use a different model (must update dimension too)
# RHP_EMBEDDING__MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
# RHP_EMBEDDING__DIMENSION=384

# Vector dimension (must match embedding model output)
# 384 = MiniLM models, 768 = BERT-base, 1024 = nomic-v1.5
# IMPORTANT: Must match RHP_EMBEDDING__DIMENSION
# RHP_STORAGE__VECTOR_DIMENSION=1024

# ============================================================================
# PATH OVERRIDES (for non-standard locations)
# ============================================================================

# Input directory for RHP PDF files
# RHP_PATHS__INPUT_DIR=/path/to/your/rhp/files

# Output directory for generated reports
# RHP_PATHS__OUTPUT_DIR=/path/to/your/reports

# Data directory for processed files
# RHP_PATHS__DATA_DIR=/path/to/your/data

# Logs directory
# RHP_PATHS__LOGS_DIR=/path/to/your/logs

# Qdrant vector database path
# RHP_STORAGE__QDRANT_PATH=/path/to/qdrant/data

# SQLite database path
# RHP_STORAGE__SQLITE_PATH=/path/to/database.db

# ============================================================================
# LLM MODEL OVERRIDES (advanced)
# ============================================================================

# Override context model (for large documents)
# RHP_LLM__CONTEXT_MODEL=mistralai/Mixtral-8x22B-Instruct-v0.1

# Override reasoning model (for analysis)
# RHP_LLM__REASONING_MODEL=meta-llama/Llama-3.3-70B-Instruct

# LLM temperature (0.0-2.0, lower = more deterministic)
# RHP_LLM__TEMPERATURE=0.1

# LLM request timeout in seconds
# RHP_LLM__TIMEOUT=180

# Maximum tokens per LLM request
# RHP_LLM__MAX_TOKENS=8192

# ============================================================================
# INGESTION OVERRIDES (document processing)
# ============================================================================

# Chunk size for document splitting (tokens)
# RHP_INGESTION__CHUNK_SIZE=1500

# Chunk overlap (tokens)
# RHP_INGESTION__CHUNK_OVERLAP=150

# Embedding batch size (higher = faster, more memory)
# RHP_INGESTION__BATCH_SIZE=64

# ============================================================================
# AGENT OVERRIDES (analysis behavior)
# ============================================================================

# Enable/disable parallel agent execution
# RHP_AGENTS__PARALLEL_EXECUTION=false

# Maximum self-critic revision cycles
# RHP_AGENTS__MAX_REVISIONS=3

# ============================================================================
# REPORTING OVERRIDES (output customization)
# ============================================================================

# Report output formats (comma-separated)
# RHP_REPORTING__FORMATS=markdown,pdf,json

# Report template name
# RHP_REPORTING__TEMPLATE=custom_template

# Include appendices in reports (true/false)
# RHP_REPORTING__INCLUDE_APPENDICES=false

# ============================================================================
# STORAGE OVERRIDES (database settings)
# ============================================================================

# Qdrant collection name
# RHP_STORAGE__QDRANT_COLLECTION=custom_collection_name

# Embedding batch size for vector operations
# RHP_EMBEDDING__BATCH_SIZE=64

# Normalize embeddings (true/false)
# RHP_EMBEDDING__NORMALIZE_EMBEDDINGS=false

# ============================================================================
# NOTES
# ============================================================================
#
# Priority Order (highest to lowest):
#   1. Command-line arguments
#   2. Environment variables (this file)
#   3. config.yaml
#   4. Built-in defaults
#
# Sensitive Data:
#   - Keep .env file out of version control (it's in .gitignore)
#   - Never commit actual tokens or API keys
#   - Use simple placeholders in .env.example (like above)
#
# Testing Different Configurations:
#   - Create multiple .env files: .env.development, .env.production
#   - Load specific file: export $(cat .env.development | xargs)
#   - Or use direnv for automatic environment switching
#
# Windows Users:
#   - Use PowerShell: $env:HF_TOKEN="your_token"
#   - Or use .env file with python-dotenv (automatic loading)
#
# Getting Help:
#   - See README.md for setup instructions
#   - See docs/user-guide.md for configuration details
#   - Check logs/ directory for error details
